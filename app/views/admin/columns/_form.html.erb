<%= form_with(model: admin_column, local: true) do |form| %>
  <% if admin_column.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_column.errors.count, "error") %> prohibited this admin_column from being saved:</h2>

      <ul>
      <% admin_column.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
